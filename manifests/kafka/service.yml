apiVersion: v1
kind: Service
metadata:
  name: kafka
  namespace: microservices-eCommerce
spec:
  type: ClusterIP
  selector:
    app: kafka
  ports:
    - name: broker
      protocol: TCP
      port: 9092
      targetPort: 9092
