apiVersion: v1
kind: Service
metadata:
  name: api-gateway-service
  namespace: microservices-eCommerce
spec:
  type: ClusterIP
  selector:
    app: api-gateway
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
