apiVersion: v1
kind: Service
metadata:
  name: orders-service
  namespace: microservices-eCommerce
spec:
  type: ClusterIP
  selector:
    app: orders-service
  ports:
    - name: http
      protocol: TCP
      port: 8082
      targetPort: 8082
