apiVersion: v1
kind: Service
metadata:
  name: products-service
  namespace: microservices-eCommerce
spec:
  type: ClusterIP
  selector:
    app: products-service
  ports:
    - name: http
      protocol: TCP
      port: 8083
      targetPort: 8083
