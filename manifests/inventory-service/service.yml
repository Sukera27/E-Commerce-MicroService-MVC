apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: microservices-eCommerce
spec:
  type: ClusterIP
  selector:
    app: inventory-service
  ports:
    - name: http
      protocol: TCP
      port: 8081
      targetPort: 8081
