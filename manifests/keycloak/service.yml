apiVersion: v1
kind: Service
metadata:
  name: keycloak
  namespace: microservices-eCommerce
spec:
  type: ClusterIP
  selector:
    app: keycloak
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
